# Telegram Vercel Function - Инструкция по развертыванию

## 📦 Что входит в комплект

Эта функция позволяет отправлять сообщения и файлы в Telegram каналы из любого проекта через простой HTTP API.

**Возможности:**
- Отправка текстовых сообщений с HTML форматированием
- Загрузка одного или нескольких файлов
- Поддержка нескольких проектов (один деплой обслуживает несколько проектов)
- Конфигурация через переменные окружения

---

## 🚀 Быстрый старт

### 1. Что потребуется

- Аккаунт Vercel (бесплатный): https://vercel.com/signup
- Telegram Bot Token (получить у @BotFather)
- Telegram канал (куда будут отправляться сообщения)

### 2. Настройка Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота (выглядит так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. Получение ID канала

**Для публичных каналов:**
- Используйте username канала: `@your_channel`

**Для приватных каналов:**
1. Добавьте [@userinfobot](https://t.me/userinfobot) в ваш канал
2. Перешлите любое сообщение из канала боту
3. Бот ответит ID канала (например: `-1001234567890`)

### 4. Добавление бота в канал

1. Откройте настройки вашего Telegram канала
2. Перейдите в **Администраторы**
3. Добавьте вашего бота как администратора
4. Дайте разрешение на публикацию сообщений

---

## 📤 Развертывание на Vercel

### Вариант 1: Через Vercel CLI (Рекомендуется)

1. **Установите Vercel CLI:**
```bash
npm install -g vercel
```

2. **Распакуйте zip файл** в папку

3. **Перейдите в папку:**
```bash
cd путь/к/распакованной/папке
```

4. **Установите зависимости:**
```bash
npm install
```

5. **Войдите в Vercel:**
```bash
vercel login
```

6. **Разверните:**
```bash
vercel --prod
```

7. **Скопируйте URL развертывания** (например: `https://your-project.vercel.app`)

### Вариант 2: Через Vercel Dashboard

1. Перейдите на https://vercel.com/new
2. Загрузите распакованную папку или подключите Git репозиторий
3. Vercel автоматически определит конфигурацию
4. Нажмите **Deploy**

---

## ⚙️ Настройка переменных окружения

После развертывания нужно настроить переменную окружения `TELEGRAM_CONFIGS`:

### 1. Откройте Vercel Dashboard

- Перейдите в ваш проект
- Откройте **Settings** → **Environment Variables**

### 2. Добавьте переменную

**Имя переменной:**
```
TELEGRAM_CONFIGS
```

**Значение (JSON массив):**
```json
[
  {
    "id": "project-1",
    "botToken": "1234567890:ABCdefGHIjklMNOpqrsTUVwxyz",
    "channelId": "@my_channel"
  },
  {
    "id": "project-2",
    "botToken": "9876543210:XYZwvuTSRqponMLKjiHGFedcba",
    "channelId": "-1001234567890"
  }
]
```

**Важно:**
- Весь JSON должен быть в **одну строку** (минифицирован)
- Выберите все окружения: **Production**, **Preview**, **Development**

**Пример (минифицированный):**
```
[{"id":"project-1","botToken":"1234567890:ABCdefGHIjklMNOpqrsTUVwxyz","channelId":"@my_channel"}]
```

### 3. Передеплойте

После добавления переменной окружения:
- Перейдите на вкладку **Deployments**
- Нажмите три точки на последнем развертывании
- Выберите **Redeploy**

---

## 📝 Использование API

### Endpoint

```
POST https://your-project.vercel.app/api/send-to-telegram
```

### Формат запроса

```json
{
  "projectId": "project-1",
  "message": "Ваше сообщение",
  "files": [
    {
      "filename": "report.txt",
      "content": "base64_encoded_file_content"
    }
  ]
}
```

**Параметры:**
- `projectId` (обязательно): ID вашего проекта из TELEGRAM_CONFIGS
- `message` (опционально): Текстовое сообщение для отправки
- `files` (опционально): Массив файлов для загрузки
  - `filename`: Имя файла
  - `content`: Содержимое файла в base64
  - `mimeType`: MIME тип (опционально)

### Примеры

**1. Отправка текстового сообщения (cURL):**
```bash
curl -X POST https://your-project.vercel.app/api/send-to-telegram \
  -H "Content-Type: application/json" \
  -d '{
    "projectId": "project-1",
    "message": "Деплой успешен! 🚀"
  }'
```

**2. JavaScript/Node.js:**
```javascript
const sendToTelegram = async (projectId, message, files = []) => {
  const response = await fetch('https://your-project.vercel.app/api/send-to-telegram', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ projectId, message, files })
  });
  return response.json();
};

// Использование
await sendToTelegram('project-1', 'Привет из моего приложения!');
```

**3. Отправка сообщения с файлом:**
```javascript
const fs = require('fs');

const fileContent = fs.readFileSync('report.txt');
const base64Content = fileContent.toString('base64');

await sendToTelegram('project-1', 'Отчет во вложении', [
  {
    filename: 'report.txt',
    content: base64Content
  }
]);
```

**4. Python:**
```python
import requests
import base64

def send_to_telegram(project_id, message, file_path=None):
    data = {
        "projectId": project_id,
        "message": message
    }
    
    if file_path:
        with open(file_path, 'rb') as f:
            content = base64.b64encode(f.read()).decode()
            data['files'] = [{
                'filename': file_path,
                'content': content
            }]
    
    response = requests.post(
        'https://your-project.vercel.app/api/send-to-telegram',
        json=data
    )
    return response.json()

# Использование
send_to_telegram('project-1', 'Привет из Python!')
```

**5. HTML форматирование:**
```javascript
const message = `
<b>Жирный текст</b>
<i>Курсив</i>
<code>Код</code>
<a href="https://example.com">Ссылка</a>
`;

await sendToTelegram('project-1', message);
```

---

## ✅ Формат ответа

**Успешный ответ:**
```json
{
  "success": true,
  "projectId": "project-1",
  "results": [
    {
      "type": "message",
      "result": {
        "ok": true,
        "result": {
          "message_id": 123,
          "text": "Ваше сообщение"
        }
      }
    },
    {
      "type": "file",
      "filename": "report.txt",
      "result": {
        "ok": true,
        "result": {
          "message_id": 124,
          "document": {
            "file_name": "report.txt",
            "file_size": 1024
          }
        }
      }
    }
  ]
}
```

**Ответ с ошибкой:**
```json
{
  "error": "Project configuration not found for ID: project-1"
}
```

---

## 🔧 Тестирование

После развертывания и настройки:

1. **Тест через cURL:**
```bash
curl -X POST https://your-project.vercel.app/api/send-to-telegram \
  -H "Content-Type: application/json" \
  -d '{"projectId":"project-1","message":"Тестовое сообщение"}'
```

2. **Проверьте ваш Telegram канал** - вы должны увидеть сообщение!

---

## 🛠️ Решение проблем

### "TELEGRAM_CONFIGS environment variable is not set"
- Убедитесь, что вы добавили переменную окружения в Vercel Dashboard
- Проверьте, что она включена для Production окружения
- Передеплойте после добавления переменной

### "Project configuration not found"
- Проверьте, что `projectId` в запросе совпадает с `id` в TELEGRAM_CONFIGS
- Убедитесь, что JSON в TELEGRAM_CONFIGS валидный

### "Telegram API error"
- Проверьте правильность токена бота
- Убедитесь, что бот добавлен как админ в канал
- Проверьте корректность ID канала (с @ для публичных, или числовой для приватных)

### Ошибки сборки
- Запустите `npm install` перед развертыванием
- Убедитесь, что все файлы из архива распакованы

---

## 📧 Поддержка

Если нужна помощь или хотите добавить больше проектов в конфигурацию, свяжитесь с администратором.

---

## 🔒 Безопасность

- Храните токены ботов в безопасности
- Никогда не коммитьте токены в систему контроля версий
- Используйте переменные окружения для чувствительных данных
- При необходимости добавьте аутентификацию к API

---

**Готово! Ваша Telegram функция готова к использованию.** 🎉
